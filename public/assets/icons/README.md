# ClipLab Icons

把你的正式软件图标放到这个目录。

推荐文件名：

- `app-icon.png`
  - 开发环境左上角品牌图标
  - Electron 窗口图标
  - macOS Dock 图标
- `app-icon.icns`
  - 以后用于 macOS 打包
- `app-icon.ico`
  - 以后用于 Windows 打包

当前仓库已经自带一个占位版 `app-icon.png`，你直接覆盖同名文件即可。
