# ClipLab Icons

把你的正式软件图标放到这个目录。

唯一需要维护的源图文件：

- `app-icon.png`
  - 开发环境左上角品牌图标
  - Electron 窗口图标
  - macOS Dock 图标
  - 打包时自动生成 `.icns` / `.ico`

要求：

- 建议使用正方形 PNG
- 建议尺寸不小于 `1024x1024`

当前仓库已经自带一个占位版 `app-icon.png`，直接覆盖同名文件即可。
